<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Canım Ablama Sürpriz</title>
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Poppins:wght@300;400;600&display=swap');

        body {
            background: linear-gradient(135deg, #fff5f5 0%, #fed7e2 100%);
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            overflow-x: hidden;
        }

        /* Giriş Paneli */
        #auth-ui {
            text-align: center;
            background: white;
            padding: 40px;
            border-radius: 30px;
            box-shadow: 0 20px 40px rgba(255, 105, 180, 0.2);
            transition: 0.5s;
            max-width: 350px;
            width: 90%;
        }

        .envelope-icon {
            font-size: 80px;
            margin-bottom: 10px;
            display: inline-block;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {transform: translateY(0);}
            40% {transform: translateY(-20px);}
            60% {transform: translateY(-10px);}
        }

        h3 { color: #d53f8c; margin-bottom: 20px; }

        input {
            padding: 12px;
            width: 80%;
            border: 2px solid #fbb6ce;
            border-radius: 15px;
            outline: none;
            font-size: 1rem;
            text-align: center;
            transition: 0.3s;
        }

        input:focus { border-color: #d53f8c; }

        button {
            padding: 12px 30px;
            background: #d53f8c;
            color: white;
            border: none;
            border-radius: 15px;
            cursor: pointer;
            margin-top: 20px;
            font-weight: bold;
            box-shadow: 0 4px 15px rgba(213, 63, 140, 0.3);
            transition: 0.3s;
        }

        button:hover { transform: translateY(-3px); background: #b83280; }

        /* Mesaj İçeriği */
        #secret-content {
            display: none;
            background: #ffffff;
            width: 90%;
            max-width: 550px;
            padding: 50px;
            border-radius: 20px;
            box-shadow: 0 25px 50px rgba(0,0,0,0.1);
            position: relative;
            background-image: radial-gradient(#fed7e2 1px, transparent 1px);
            background-size: 20px 20px;
            border: 2px solid #fbb6ce;
        }

        h2 { 
            font-family: 'Dancing Script', cursive;
            color: #b83280; 
            font-size: 2.5rem;
            margin-top: 0;
            text-align: center;
        }

        .long-message {
            color: #4a5568;
            line-height: 1.8;
            font-size: 1.1rem;
            text-align: left;
            white-space: pre-line;
            border-top: 1px solid #fed7e2;
            padding-top: 20px;
        }

        .signature {
            font-family: 'Dancing Script', cursive;
            font-size: 1.8rem;
            color: #d53f8c;
            text-align: right;
            margin-top: 30px;
        }

        .heart-badge {
            position: absolute;
            top: -25px;
            right: -25px;
            background: #ff4d6d;
            color: white;
            width: 60px;
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            font-size: 30px;
            transform: rotate(15deg);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
    </style>
</head>
<body>

    <div id="auth-ui">
        <div class="envelope-icon">✉️</div>
        <h3>Canım Ablama...</h3>
        <p style="color: #718096; font-size: 0.9rem;">Sana özel bu mektubu açmak için şifreyi gir:</p>
        <input type="text" id="password" placeholder="Şifreyi buraya yaz...">
        <br>
        <button onclick="checkPassword()">Mektubu Oku ❤️</button>
    </div>

    <div id="secret-content">
        <div class="heart-badge">❤️</div>
        <h2>Canım Ablam, ✨</h2>
        <div class="long-message">
            Bazen sana bunu yeterince söyleyemiyorum ama iyi ki varsın. Hayatımda senin gibi birinin olması gerçekten büyük bir şans. Ne zaman zor bir an yaşasam, bilmesen bile varlığın bana güç veriyor. Sen sadece ablam değil, aynı zamanda yol göstericim, sırdaşım ve en güvenli limanımsın.

            Gülüşün evin havasını değiştiriyor, konuşman içimi rahatlatıyor. Küçükken belki fark etmiyordum ama büyüdükçe senin kıymetini daha iyi anlıyorum. Sabırla beni dinlemen, bazen kızsan bile arkamda durman… Bunların hepsi benim için çok değerli.

            Sen güçlü birisin. Hem kalbi güzel hem de içi tertemiz birisin. Hayatta ne yaparsan yap, nereye gidersen git, bil ki seni seven ve seninle gurur duyan bir kardeşin var. Her zaman yanında olacağım.

            İyi ki benim ablam olmuşsun. Seni çok seviyorum.
        </div>
        <div class="signature">Kardeşin 💖</div>
    </div>

    <script>
        function checkPassword() {
            const pass = document.getElementById('password').value;
            // ŞİFRE: Ponçik
            if (pass.toLowerCase() === "ponçik" || pass === "Ponçik") {
                document.getElementById('auth-ui').style.display = 'none';
                const content = document.getElementById('secret-content');
                content.style.display = 'block';

                // Kutlama Şöleni
                fireworks();
            } else {
                alert("Yanlış şifre! İpucu: P... ile başlıyor 😊");
            }
        }

        function fireworks() {
            var duration = 10 * 1000;
            var animationEnd = Date.now() + duration;

            var interval = setInterval(function() {
                var timeLeft = animationEnd - Date.now();
                if (timeLeft <= 0) return clearInterval(interval);

                // Renkli konfetiler ve kalpler
                confetti({
                    particleCount: 5,
                    angle: 60,
                    spread: 55,
                    origin: { x: 0 },
                    colors: ['#ff4d6d', '#ff8fa3', '#ffb3c1', '#ffffff']
                });
                confetti({
                    particleCount: 5,
                    angle: 120,
                    spread: 55,
                    origin: { x: 1 },
                    colors: ['#ff4d6d', '#ff8fa3', '#ffb3c1', '#ffffff']
                });
            }, 150);
        }
    </script>
</body>
</html>
